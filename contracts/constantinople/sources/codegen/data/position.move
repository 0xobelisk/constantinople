  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: Apache-2.0
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module constantinople::position {

  use std::ascii::String;

  use constantinople::monster_type::MonsterType;

  use constantinople::direction::Direction;

  use constantinople::terrain_type::TerrainType;

  use constantinople::monster_catch_result::MonsterCatchResult;

  public struct Position has copy, drop, store {
    x: u64,
    y: u64,
  }

  public fun new(x: u64, y: u64): Position {
    Position {
                                   x,y
                               }
  }

  public fun get(self: &Position): (u64,u64) {
    (self.x,self.y)
  }

  public fun get_x(self: &Position): u64 {
    self.x
  }

  public fun get_y(self: &Position): u64 {
    self.y
  }

  public(package) fun set_x(self: &mut Position, x: u64) {
    self.x = x;
  }

  public(package) fun set_y(self: &mut Position, y: u64) {
    self.y = y;
  }

  public(package) fun set(self: &mut Position, x: u64, y: u64) {
    self.x = x;
    self.y = y;
  }
}
